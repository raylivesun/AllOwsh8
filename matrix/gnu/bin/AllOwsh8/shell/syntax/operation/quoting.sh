#!/usr/bin/sh

QUOTING_FILE="$(dirname $0)/../../../"

if [ -n "$QUOTING_FILE" ]; then
    . "$QUOTING_FILE/quoting.sh"
fi

if [ -n "$QUOTING_FILE" ]; then
    . "$QUOTING_FILE/quoting.sh"
fi

if [ -n "$QUOTING_FILE" ]; then
    . "$QUOTING_FILE/quoting.sh"
fi
